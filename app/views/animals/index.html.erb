  <p id="notice"><%= notice %></p>

<h1>Animals</h1>

<table id="animal_list">
  <thead>
    <tr>
      <th>Common name</th>
      <th>Latin name</th>
      <th>Kingdom</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @animals.each do |animal| %>
      <tr>
        <td><%= animal.common_name %></td>
        <td><%= animal.latin_name %></td>
        <td><%= animal.kingdom %></td>
        <td><%= link_to 'Show', animal %></td>
        <td><%= link_to 'Edit', edit_animal_path(animal) %></td>
        <td><%= link_to 'Destroy', animal, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Animal Sightings', sightings_path %> | <%= link_to 'Create New Animal', new_animal_path %>

<hr>

<h3>Add an Animal with AJAX</h3>
<p>
  <!-- Input area for new animal -->
  <div class="field">
    <label for="common_name">Common Name</label>
    <input type="text" name="animal[common_name]" id="common_name" />
  </div>
  <div class="field">
    <label for="latin_name">Latin Name</label>
    <input type="text" name="animal[latin_name]" id="latin_name" />
  </div>
  <div class="field">
    <label for="kingdom">Kingdom</label>
    <input type="text" name="animal[kingdom]" id="kingdom" />
  </div>

  <button id="add_new_animal_button">Create New Animal</button>
</p>
